apiVersion: v2
name: pod-identity
description: A helm-chart to deploy pod-identity on AKS
type: application
version: 0.1.0

# Chart.yaml
dependencies:
- name: aad-pod-identity
  version: ~2.0.0
  repository: "https://raw.githubusercontent.com/Azure/aad-pod-identity/master/charts"
